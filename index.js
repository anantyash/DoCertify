/**
 * @format
 */

import {AppRegistry} from 'react-native';
import './gesture-handler';
import {name as appName} from './app.json';
import DoCertify from './App';

AppRegistry.registerComponent(appName, () => DoCertify);
